- defaults:
    link:
      relink: true
      create: true

- clean: ['~']

- link:
    ~/.bashrc:
    ~/.bash_logout:
    ~/.gitconfig:
    ~/.profile:
    ~/.tmux.conf:
    ~/.config/fish:
    ~/.config/nvim:
    ~/.config/VSCodium:
    ~/.local/share/fonts:
    # ~/.zshrc: zshrc
    # ~/.vimrc: vimrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]

